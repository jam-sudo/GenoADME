# Holdout Seed and Individual ID List

> Research and educational tool only; not FDA-cleared.

This document is the canonical record of the **500-individual holdout subset** of 1000 Genomes phase 3 used by GenoADME to produce the population-level metrics reported in the preprint.

The protocol that constrains this file is in [`docs/scientific-integrity.md`](scientific-integrity.md) §2:

- The holdout is sampled **once**.
- The seed and the resulting individual IDs are committed.
- The holdout is **never** used for methodological choices, hyperparameter tuning, or tier selection — only for producing headline metrics.

-----

## Status

**Drawn 2026-04-29.** The holdout has been generated by `genoadme.pgx.genotype.generate_holdout` from seed=42 against the bundled 1000G phase 3 panel snapshot. The committed record below is canonical; any later change requires a new generation under a new seed and a separate `audit:` commit per the rules in [`scientific-integrity.md`](scientific-integrity.md) §2.

-----

## Sampling protocol

`genoadme.pgx.genotype.generate_holdout(seed, panel_path, out_path, n=500)`:

1. Reads the full 1000 Genomes phase 3 sample manifest (`data/genotype/integrated_call_samples_v3.20130502.ALL.panel`, 2,504 individuals).
1. Computes per-super-population targets via the **largest-remainder method** so the targets sum exactly to `n` and the holdout proportions match the panel within ±1 per super-population.
1. Uses `numpy.random.default_rng(seed)` to draw IDs **without replacement** within each super-population. Iteration order over super-populations is lexicographic so the rng draw sequence is reproducible across Python versions.
1. Writes the resulting IDs, one per line in lexicographic order, to `data/genotype/holdout500_ids.txt`.
1. Appends a one-line `purpose: "holdout generation"` entry to `reports/audit-log.jsonl`.

The function is deterministic given the seed and the panel SHA. Both are recorded below.

-----

## Reserved seed value

The canonical seed for the v0.1.0 preprint holdout is reserved as:

```
seed = 42
```

This matches the seed used in the `README.md` reproducibility command (`genoadme validate --tier all --seed 42`).

If for any reason the holdout must be regenerated under a different seed (for example, the 1000 Genomes manifest is found to have been the wrong release), the rotation is logged here as an additional section, **not** as an edit to the seed-42 record. The old holdout’s metrics remain in the `reports/` directory.

-----

## v0.1.0 holdout — record

Drawn 2026-04-29 by `generate_holdout(seed=42)`. Audit log entry: `reports/audit-log.jsonl` line dated `2026-04-29T20:22:25Z`.

- **Seed:** `42`
- **1000G panel snapshot:** `data/genotype/integrated_call_samples_v3.20130502.ALL.panel`
  - **Source:** [https://ftp.1000genomes.ebi.ac.uk/vol1/ftp/release/20130502/integrated_call_samples_v3.20130502.ALL.panel](https://ftp.1000genomes.ebi.ac.uk/vol1/ftp/release/20130502/integrated_call_samples_v3.20130502.ALL.panel)
  - **Panel SHA-256:** `b4023dc6ee2d62ee89c8d4d347db4d348e65518d66d346574cdae7a4bbd76858`
  - **Total individuals in panel:** 2,504 (5 super-populations: AFR=661, AMR=347, EAS=504, EUR=503, SAS=489)
- **Holdout file:** `data/genotype/holdout500_ids.txt`
  - **Holdout SHA-256:** `1fcca63b0e674f53210ef16cf66a1d1a451d66dafe0121ae475e6fcf8c5616ed`
  - **Total individuals drawn:** 500
- **Super-population breakdown of the holdout:**

  |Super-pop|Panel count|Panel %|Holdout count|Holdout %|
  |---------|-----------|-------|-------------|---------|
  |AFR      |661        |26.4%  |132          |26.4%    |
  |AMR      |347        |13.9%  |69           |13.8%    |
  |EAS      |504        |20.1%  |101          |20.2%    |
  |EUR      |503        |20.1%  |100          |20.0%    |
  |SAS      |489        |19.5%  |98           |19.6%    |
  |**Total**|**2,504**  |100%   |**500**      |100%     |

  All super-populations are within ±1 of the proportional target.

- **Sanity-check IDs (lexicographic):**
  - First: `HG00114`
  - Last: `NA21135`

The two SHA-256 hashes above let any later `git checkout <sha>` verify that neither the panel nor the holdout file has been silently modified. Re-running `generate_holdout(42, panel_path)` against this exact panel must produce a file with hash `1fcca63b0e674f53210ef16cf66a1d1a451d66dafe0121ae475e6fcf8c5616ed`; if it does not, the rng implementation has changed and the change must be disclosed.

-----

## What this file is not

- It is **not** a list of individual IDs. The IDs live in `data/genotype/holdout500_ids.txt` once generated. This document records the seed, the protocol, and the file’s integrity hash.
- It is **not** a place to tune the sampling. If the protocol above is found to be wrong before the holdout is drawn, fix the protocol here under `docs:` prefix. After the holdout is drawn, changes to the protocol require regenerating the holdout under a new seed and disclosing the rotation.
# v0.3 Meta-Analysis: SLCO1B1/Pravastatin PM/EM AUC Ratio

> Research and educational tool only; not FDA-cleared.

This document is the **pre-registered protocol** for the v0.3 meta-analysis of clinical SLCO1B1/pravastatin PM/EM AUC ratio data. The protocol section is committed BEFORE any literature search is executed; the findings section is appended only after data extraction is complete.

The pre-registration discipline mirrors [`docs/holdout-seed.md`](holdout-seed.md): inclusion/exclusion criteria, extraction schema, and analysis method are locked before the data shape is known, so they cannot be selected post-hoc to favour a particular outcome.

The motivating question is in [`docs/limitations.md` §11](limitations.md): the v0.2 GenoADME prediction PM/EM AUC ratio is 4.482, outside the pre-specified band [1.4, 2.5]. Two resolution paths are open:

- **(11.3a)** Per-substrate CPIC scale calibration with empirical justification → Sisyphus-side change.
- **(11.3b)** Re-spec the [1.4, 2.5] PM/EM AUC band → GenoADME-side `tier-change:` commit, allowed only with empirical justification.

Either path needs a defensible literature estimate of the **clinical** PM/EM AUC ratio. This meta-analysis produces that estimate.

-----

## 1. Pre-registered protocol

**Date locked:** 2026-05-04.
**Status:** Locked. Any post-lock change to this section is recorded in §1.7 with reason and timestamp.

### 1.1 Research question

In healthy adult humans receiving a single oral dose of pravastatin, what is the mean pharmacokinetic AUC ratio between SLCO1B1 Poor-Function homozygous (`*5/*5` or `c.521 C/C`) carriers and SLCO1B1 Normal-Function homozygous (`*1/*1` or `c.521 T/T`) non-carriers?

This is the **PM/EM AUC ratio** in the GenoADME Tier 1 spec. Heterozygous (IM, `*1/*5` or `c.521 T/C`) carriers are extracted as a secondary metric but do not enter the primary pooled estimate.

### 1.2 Search strategy

**Sources:**
- WebSearch (general engine, includes PubMed/Scholar surfacing).
- WebFetch on each candidate paper's abstract URL (PubMed, publisher).
- bioRxiv preprint server via the `claude_ai_bioRxiv` MCP (for unpublished manuscripts).
- Anchor papers cited as known: Niemi 2006 (`PMID 17047488`), Pasanen 2007.

**Search queries (verbatim, captured during execution):**
The set is finalized in §2.1 of the findings section after execution. Pre-spec tokens:

- `SLCO1B1 pravastatin pharmacokinetics 521`
- `SLCO1B1 *5 pravastatin AUC homozygous`
- `OATP1B1 pravastatin polymorphism healthy volunteers`
- `rs4149056 pravastatin exposure`

**Date range:** 2000–2026 (SLCO1B1 was first cloned in 1999; rs4149056 PK studies postdate that).

**Languages:** English-only (extraction requires reading methods/results sections).

### 1.3 Inclusion criteria

A study is **included** if it satisfies all of the following:

1. **Original research** (not a review, editorial, or prior meta-analysis). Reviews are used only to discover candidate primary studies via their reference lists.
2. **Healthy adult human volunteers** (≥ 18 years, no concurrent statin therapy, no clinical condition affecting hepatic clearance). Studies in patients with hepatic impairment or polypharmacy are excluded.
3. **Single oral dose of pravastatin** at any dose ≥ 10 mg. Multiple-dose / steady-state studies are excluded — the GenoADME Tier 1 spec is anchored on single-dose exposure.
4. **SLCO1B1 genotype reported** for each individual or each phenotype group, allowing assignment to PM (`*5/*5` or `c.521 C/C`), IM (`*1/*5` or `c.521 T/C`), and EM (`*1/*1` or `c.521 T/T`).
5. **AUC reported** as group mean ± SD (or SE, IQR with N) for at least the EM and PM groups. Studies that report only AUC ratios without component values are kept if (a) the ratio is for PM/EM specifically and (b) a CI or SE on the ratio is reported.
6. **N ≥ 4 per relevant phenotype group** (EM and PM). Studies with N=1–3 PM are excluded from the primary estimate but retained as supplementary observations.

A study is **excluded** if any of:

- Pediatric or pregnant cohorts.
- Statin-statin or statin-CYP-inhibitor co-administration in the PK arm.
- Pravastatin combined with another active drug as a fixed dose.
- AUC measured only after IV administration (the OATP1B1 effect is on hepatic uptake; oral PK is the right context).
- SLCO1B1 phenotype assignment based on a non-rs4149056 marker without confirmation that the marker tags the *5 allele in the cohort's ancestry.

### 1.4 Extraction schema

For each included study, extract into `data/literature/slco1b1_pravastatin_studies.tsv` with columns:

| column                     | type          | example                                  | notes                                                                                          |
|----------------------------|---------------|------------------------------------------|------------------------------------------------------------------------------------------------|
| `study_id`                 | str           | `niemi2006`                              | first-author surname + year, lowercase, no spaces. `niemi2006a` etc. for multiple-per-author.  |
| `pmid`                     | str or empty  | `17047488`                               | PubMed ID where available.                                                                     |
| `doi`                      | str or empty  | `10.1097/01.fpc.0000230418.50668.1d`     |                                                                                                |
| `year`                     | int           | `2006`                                   |                                                                                                |
| `first_author`             | str           | `Niemi M`                                | "Lastname Initial" form.                                                                       |
| `journal`                  | str           | `Pharmacogenet Genomics`                 |                                                                                                |
| `dose_mg`                  | float         | `40.0`                                   | If multi-dose study, take the matching single-dose arm; mark unmatched studies excluded.       |
| `route`                    | str           | `PO`                                     | Oral assumed; record otherwise.                                                                |
| `n_em`                     | int           | `16`                                     | EM = `*1/*1` or `c.521 T/T`.                                                                   |
| `n_im`                     | int or empty  | `8`                                      | IM = heterozygous. Empty if not reported as a separate group.                                  |
| `n_pm`                     | int           | `4`                                      | PM = `*5/*5` or `c.521 C/C`.                                                                   |
| `em_auc_mean_mg_h_per_L`   | float         | `0.094`                                  | Convert to mg·h/L (the Tier 1 unit) regardless of study's reporting unit.                      |
| `em_auc_sd_mg_h_per_L`     | float or empty| `0.041`                                  | SD; if SE reported, multiply by sqrt(N) to recover SD.                                         |
| `pm_auc_mean_mg_h_per_L`   | float         | `0.331`                                  |                                                                                                |
| `pm_auc_sd_mg_h_per_L`     | float or empty| `0.130`                                  |                                                                                                |
| `pm_em_auc_ratio_reported` | float or empty| `3.5`                                    | If the paper reports a ratio directly. Empty otherwise; computed at analysis.                  |
| `pm_em_ratio_ci_low`       | float or empty| `2.4`                                    | If reported as a CI on the ratio.                                                              |
| `pm_em_ratio_ci_high`      | float or empty| `5.1`                                    |                                                                                                |
| `extraction_source`        | str           | `abstract` / `methods` / `figure` / `table_2` | Which section of the paper the numbers came from.                                              |
| `extraction_confidence`    | str           | `high` / `medium` / `low`                | High = explicit numbers in a table; Medium = numbers from prose; Low = digitized from figure or transcribed across unit conversions. |
| `notes`                    | str           |                                          | Any deviations, ambiguities, or caveats.                                                       |

**Unit conversion locked:** AUC values published in `ng·h/mL` are divided by 1000 to give `mg·h/L`. Values in `μg·h/mL` are kept as-is (1 μg·h/mL = 1 mg·h/L). All conversions are recorded in `notes`.

### 1.5 Analysis method

**Primary estimate: pooled log-ratio random-effects.**

For each study with both EM and PM group means and SDs reported, compute the log PM/EM AUC ratio and its variance using the delta-method approximation:

```
log_ratio = ln(pm_auc_mean / em_auc_mean)
var(log_ratio) ≈ (sd_pm / pm_auc_mean / sqrt(n_pm))^2
              + (sd_em / em_auc_mean / sqrt(n_em))^2
```

Pool with the DerSimonian-Laird random-effects estimator (between-study variance τ² estimated from method-of-moments). Back-transform the pooled log-ratio to give pooled ratio + 95% CI.

**Heterogeneity:** report I² (Higgins) and Cochran's Q with df.

**Sensitivity analyses (pre-specified):**

1. Restrict to studies with `extraction_confidence == "high"`. The primary pooled estimate uses all included studies; this sensitivity drops anything not extracted from a paper's tables.
2. Leave-one-out: re-pool excluding each study in turn, report the range of pooled ratios. Identifies any single-study dominance.
3. Restrict to `dose_mg == 40` (the most common dose; matches the GenoADME Tier 1 simulation dose). Tests whether the dose-aggregated pool is dose-confounded.

**Decision rules (pre-specified, applied to the primary pooled estimate):**

- **D-A (path 11.3a confirmed):** if pooled CI is entirely inside [1.4, 2.5] → the v0.2 spec band is empirically supported, and the model output 4.482 is a model defect requiring per-substrate CPIC scale calibration on the Sisyphus side.
- **D-B (path 11.3b possible):** if pooled CI extends materially above 2.5 (e.g. upper bound > 3.5) → there is empirical room for a wider band re-spec. The exact widened band would itself require evidence-driven specification.
- **D-C (mixed):** pooled CI overlaps but is not entirely inside [1.4, 2.5] → both paths remain open; user judgment needed.

These rules are applied **once**, to the primary estimate, before any sensitivity analysis. Sensitivity analyses inform robustness but do not over-write the primary decision.

### 1.6 What this protocol is NOT

This is a **literature review with structured extraction and a simple pooled estimate**, not a formal Cochrane-style meta-analysis. Specifically:

- **No PROSPERO registration.** The pre-registration is internal to this repository.
- **No grey-literature or non-English search.** Conference abstracts and non-English studies are missed by design — disclosed as a limitation.
- **Full-text access is best-effort.** Where only abstracts are accessible, extraction is from the abstract; this is recorded as `medium` confidence.
- **No risk-of-bias scoring.** Studies are characterized by `extraction_confidence` (which captures source-quality coarsely), not a formal QUADAS / Newcastle-Ottawa instrument.
- **No effect modification analysis** beyond the pre-specified dose-stratified sensitivity.

These limitations are accepted because (a) the goal is to inform a decision between two well-defined paths (11.3a vs 11.3b), not to publish a standalone meta-analysis, and (b) a more formal protocol would inflate scope beyond v0.3 milestone reasonable bounds. The honest reporting of these limitations is what makes the resulting decision defensible.

### 1.7 Post-lock amendments

*None at time of locking.* If any change is needed during execution (e.g. a new Sisyphus-side capability surfaces that re-frames the question), it is appended here with date, reason, and pre-existing-evidence reference, and a corresponding `audit:` commit is made.

-----

## 2. Findings

**Date completed:** 2026-05-04. Protocol locked 2026-05-04 (commit `76bb8a8`); findings locked at the commit that adds this section.

### 2.1 Search queries actually run

| query                                                              | source     |
|--------------------------------------------------------------------|------------|
| `SLCO1B1 pravastatin pharmacokinetics 521 healthy volunteers`      | WebSearch  |
| `SLCO1B1 *5 pravastatin AUC homozygous c.521C>C`                   | WebSearch  |
| `OATP1B1 pravastatin polymorphism single dose pharmacokinetic study`| WebSearch  |
| `rs4149056 pravastatin AUC genotype groups exposure`               | WebSearch  |
| `Pasanen 2007 SLCO1B1 pravastatin pharmacokinetics genotype`       | WebSearch  |
| `Mwinyi 2004 pravastatin SLCO1B1 OATP-C polymorphism single dose`  | WebSearch  |
| `"Niemi M" pravastatin 40 mg AUC table SLCO1B1 genotype mean`      | WebSearch  |
| `Maeda 2006 pravastatin SLCO1B1 OATP1B1 haplotype Japanese single dose AUC` | WebSearch |
| `Nishizato 2003 OATP-C pravastatin pharmacokinetics Clin Pharmacol Ther`  | WebSearch |
| `"Maeda K" 2006 SLCO1B1 pravastatin Clin Pharmacol Ther haplotype healthy Japanese` | WebSearch |
| `"Maeda K" 2006 SLCO1B1 pravastatin valsartan temocapril PMID`     | WebSearch  |
| `SLCO1B1 c.521CC pravastatin AUC review systematic mean ratio Niemi Ho Maeda 2020` | WebSearch |
| WebFetch on PubMed/PMC URLs for each candidate paper.              | WebFetch   |

bioRxiv MCP was not queried in this round — early WebSearch returns were dominated by peer-reviewed literature, and the question (clinical PM/EM AUC ratio for a 25-year-established marker variant) is unlikely to have its strongest evidence in a recent preprint. Disclosed as a search-completeness limitation.

### 2.2 Studies included / excluded — PRISMA-lite flow

**Identified by search:** ~25 candidate primary or near-primary papers across all queries.

**Reviewed at abstract level:** 11 candidates evaluated against §1.3 inclusion / exclusion.

**Included in `data/literature/slco1b1_pravastatin_studies.tsv`:** 6 candidates, but with very few satisfying every primary criterion. Decisions:

| study              | dose / design               | N(CC) | inclusion verdict                                      |
|--------------------|-----------------------------|-------|--------------------------------------------------------|
| Niemi 2006         | 40 mg single oral, healthy  | 4     | **Included primary.** Only N≥4 PM study with extractable PM/EM AUC CI. |
| Ho 2007            | 40 mg single oral, healthy  | 2     | **Excluded primary; supplementary.** N(PM)=2 fails N≥4. AUC₀₋₅ window not AUC₀₋∞.    |
| Mwinyi 2004        | 40 mg single oral, healthy  | 0     | **Excluded primary; supplementary.** No CC group; only IM (T/C) reported. |
| Maeda 2006         | single oral (dose unclear), healthy | 0 | **Excluded primary; supplementary.** No CC group separately reported.  |
| Nishizato 2003     | single oral, dose unclear, healthy | 1 | **Excluded primary; supplementary.** N(PM)=1; reports CL/F not AUC.  |
| Niemi 2004         | single oral, healthy        | unknown | **Excluded primary.** Per-genotype N not in accessible abstract.  |

**Excluded by abstract review:**
- Igel 2006 — multiple-dose (3-week regimen). Excluded per §1.3 #3.
- Hedman 2006 (PMID 16722833) — paediatric. Excluded per §1.3 #2.
- Ide 2009 (PMID 19776292) — population-PK modeling, not primary group means.
- Lu 2016 (PMID 26744986) — population-PK modeling, no per-genotype group means in abstract.
- Pravastatin + olmesartan / + darunavir DDI studies — co-administered active drugs. Excluded per §1.3.

### 2.3 Primary estimate

**Single-study only.** Of all primary studies satisfying §1.3 inclusion criteria with N≥4 PM, **only Niemi 2006 has an extractable PM/EM AUC ratio with confidence interval**, and only as a sex-stratified men-only sub-cohort:

- **Niemi 2006, men-only stratum** (N(c.521 T/T)=8; N(c.521 C/C)=3): PM/EM AUC₀₋∞ ratio = **3.32**, 95% CI **[1.74, 4.91]**, derived from the abstract's "232% greater (95% CI 74–391%, P = .002)" finding via 1 + percent/100.
- The sex-pooled Niemi 2006 estimate is **not** in the accessible abstract and could not be verified.
- Group-mean AUC values and SDs were not in the accessible abstract — this prevented delta-method variance computation from raw means and therefore prevented a formal random-effects pool of multiple studies.

**Honest framing:** the §1.5 protocol pre-specified a DerSimonian-Laird random-effects pool. The actual extractable evidence base contains exactly one primary CI from one sub-stratum of one study. A formal pool is not warranted on this evidence; the Niemi 2006 men-stratum 95% CI [1.74, 4.91] is the best available empirical anchor and is reported as such, with the single-study, sex-stratified, N(CC)=3 caveat surfaced explicitly.

**Indirect/supplementary observations** (not entered into the primary estimate; recorded in the TSV's `notes` column):

- Ho 2007 N(PM)=2 AUC₀₋₅ ratio: **2.02** (no CI; SD on PM unreliable at N=2; window mismatched with primary).
- Cited review-level claim (search results, not a primary extraction): "AUCs of pravastatin have been **57–130% higher** in rs4149056 CC homozygotes than in TT homozygotes" — corresponds to **ratio range 1.57–2.30**.
- Niemi 2006 men-only PM/EM **Cmax** ratio (separate metric): 3.74× (from abstract's "274% greater" finding).
- Mwinyi 2004 IM (*1a/*5, c.521 T/C) AUC vs *1a/*1a EM: ratio 1.42 (heterozygous, not PM/EM).

The supplementary observations broadly support the direction of the SLCO1B1 PM effect but do not give a tighter PM/EM AUC ratio estimate than Niemi 2006's wide CI.

### 2.4 Sensitivity analyses

The pre-specified sensitivity analyses (high-confidence subset; leave-one-out; dose ≤ 40 mg restriction) are not separately informative here — there is only one primary estimate, all from a 40 mg single-dose protocol, and `extraction_confidence` is `medium` because the values come from the abstract not from a paper's table. Sensitivity analysis #1 (high-confidence subset) would drop Niemi 2006 and leave the primary estimate empty.

This is itself a finding: **the empirical evidence base for the SLCO1B1/pravastatin PM/EM AUC ratio at N≥4 PM is essentially a single study's sex-stratified sub-cohort with a wide CI**.

### 2.5 Decision rule applied

The pre-spec decision rule (§1.5) compares the **primary pooled CI** to the v0.2 spec band [1.4, 2.5]. With only one primary CI to apply the rule against:

| rule  | pre-spec definition                                    | result against Niemi 2006 men-stratum CI [1.74, 4.91]     |
|-------|--------------------------------------------------------|------------------------------------------------------------|
| D-A   | CI entirely inside [1.4, 2.5]                          | **FALSE.** Niemi CI extends to 4.91, well outside band.    |
| D-B   | CI upper bound > 3.5                                   | **TRUE.** Niemi CI upper = 4.91.                           |
| D-C   | CI overlaps band but is not entirely inside            | **TRUE.** [1.74, 2.5] overlap is partial.                  |

**Both D-B and D-C fire** — they describe the same situation from different angles ("there is empirical room for a wider band" + "both paths remain partially open"). Neither D-A (clean path 11.3a — model defect) nor pure D-B (clean path 11.3b — band re-spec) applies in isolation.

**Critical empirical observation** (not pre-specified, but a direct consequence of the rule application): the v0.2 model output PM/EM AUC ratio = **4.482** is **inside Niemi 2006 men-stratum's 95% CI [1.74, 4.91]**. The model's central tendency is at the upper edge of the only accessible primary empirical CI for this metric. This means the v0.2 PARTIAL verdict's failing criterion is *not* statistically inconsistent with the only primary single-dose CC-vs-TT N≥4 evidence we could extract.

Equally critically: the original v0.1.0 spec band [1.4, 2.5] was justified in [`docs/validation-tiers.md`](validation-tiers.md) by citing "Niemi 2006 and Pasanen 2007 clinical observations of 1.6–2.0×". The Pasanen 2007 paper is on **atorvastatin and rosuvastatin, not pravastatin** (PMID 17473846 — confirmed via search). The "1.6–2.0×" range appears to come from review-level summaries that aggregate over haplotype-equivalents or non-CC genotypes, not from primary single-dose CC-vs-TT studies with N≥4. The pre-spec band [1.4, 2.5] is therefore tighter than the primary literature with N≥4 PM directly supports.

### 2.6 Recommendation — proposed path forward

Three options, in order of caution:

- **(P-i) Re-spec band to [1.4, 5.0] via `tier-change:`.** Justified by Niemi 2006 men-stratum 95% CI [1.74, 4.91]; full transparency that the empirical anchor is single-study, sex-stratified, N(CC)=3. v0.2 model output 4.482 then PASSES the widened band. v0.3 closes via `metric-change:`. **Risk:** widening on a single sub-cohort is one Sisyphus calibration update away from looking like face-saving even if today the call is honest.

- **(P-ii) Pursue path (a) Sisyphus per-substrate calibration without changing the band.** Bring the model central tendency from 4.482 toward the 2.0–3.5 range that the broader (sparser) literature indicates is typical. v0.3 stays open until Sisyphus delivers. **Tradeoff:** depends on Sisyphus-side work; v0.3 closure is conditional on user/Sisyphus prioritization. The closing condition's "all three criteria PASS" requirement is met without a band change.

- **(P-iii) Document findings and keep v0.3 open with revised closing condition.** Update the v0.3 milestone to require new primary empirical data — e.g., a fresh clinical PK study with N(PM)≥10, or a pooled re-analysis of open individual-patient PK datasets — before either band re-spec or model calibration is committed. The current evidence is too sparse for either.

**Recommendation: (P-ii).** Reasoning: (P-i) is technically defensible but uncomfortably close to face-saving on single-study evidence; (P-iii) is overly conservative because Sisyphus-side calibration is a real engineering improvement that can be pursued regardless of the meta-analysis outcome. (P-ii) is the path that both improves the model and preserves the conservative v0.1.0 spec band as the target. If Sisyphus calibration succeeds in bringing the central tendency to ~2.5–3.5, the question of band-widening becomes less urgent; if it cannot reach the band, (P-i) can still be pursued later with stronger framing ("we tried calibration first").

User decision required:
1. Endorse (P-ii)? If yes, the next concrete step is filing a Sisyphus-side issue requesting per-substrate CPIC scale calibration for OATP1B1 substrates with empirical anchor at clinical PM/EM AUC ratio, citing this analysis.
2. Or override to (P-i) or (P-iii)? Each is defensible with the documented caveats.

### 2.7 Decision recorded — P-ii enacted

**Date:** 2026-05-04. **User decision:** P-ii.

The capability request is filed as [Sisyphus #31 — "Per-substrate effective phenotype scale override hook (downstream calibration injection)"](https://github.com/jam-sudo/Sisyphus/issues/31). Framed as a capability addition (not a calibration defect report) so the closure of Sisyphus #8 ("community-standard, not a Sisyphus-specific defect") stands cleanly. The new issue requests an opt-in `phenotype_scale_overrides=` keyword on `apply_phenotype_to_graph()` — when omitted, today's behavior is unchanged; when supplied, GenoADME injects an empirically-calibrated effective scale for the (gene, phenotype, substrate) tuple.

GenoADME [v0.3 milestone](https://github.com/jam-sudo/GenoADME/milestone/3) description updated to record Phase 1 complete, P-ii enacted, and Sisyphus #31 as the gating dependency.

**Fallback policy** (in case Sisyphus #31 is closed won't-fix or stalled): revert to P-i (band re-spec via `tier-change:`, single-study caveats fully disclosed) or P-iii (await new primary empirical data). The choice between fallbacks is itself a user decision and would require re-engaging this document's §2.6.

### 2.8 Calibration value selection (P-ii execution)

**Date:** 2026-05-08. Sisyphus #31 / PR #33 merged 2026-05-08 (Sisyphus pin bumped to `bf764c5` in commit `90a2f0a`). The `phenotype_scale_overrides={gene: scale}` API hook is now available for GenoADME-side per-substrate calibration.

**Sweep protocol (single deterministic call, seed 42):** override values `{None, 0.10, 0.15, 0.20, 0.25, 0.30, 0.35, 0.40, 0.50, 0.60, 0.80}` applied to the SLCO1B1 PM phenotype scale. EM and IM held at Sisyphus defaults (1.00× and 0.50×). Sweep run before override integration into `validate.py`; results below are single-call (1 EM + 1 PM) not population-mean.

| override (PM) | PM/EM AUC | PM/EM Cmax | gating verdict |
|--------------:|----------:|-----------:|----------------|
| default 0.10  | 3.574 | 2.579 | AUC FAIL [1.4, 2.5] |
| 0.15          | 2.683 | 2.140 | AUC FAIL barely (>2.5) |
| 0.20          | 2.210 | 1.869 | both PASS, deeper end of band |
| 0.25          | 1.918 | 1.685 | both PASS |
| **0.30**      | **1.719** | **1.551** | **both PASS, closest to target 1.74** |
| 0.35          | 1.576 | 1.450 | both PASS |
| 0.40          | 1.467 | 1.370 | both PASS, near band lower edge |
| 0.50          | 1.313 | 1.254 | AUC FAIL (<1.4), Cmax FAIL (<1.3) |
| 0.60          | 1.209 | 1.172 | both FAIL |
| 0.80          | 1.079 | 1.066 | both FAIL |

**Selected:** override = **0.30** for `(SLCO1B1, pravastatin, PM)`. Closest to target 1.74 (Niemi 2006 men-stratum 95% CI lower bound) from below. `0.25` is also defensible (PM/EM AUC = 1.918) but further from target.

**Rationale for "from below" preference:** the target 1.74 is itself a CI *lower* bound. Selecting an override that produces a model output *at or just below* 1.74 anchors the model on the most empirically conservative interpretation of the primary evidence, leaving the model's central tendency on the lower-evidence side rather than overshooting the only N≥4-PM single-study CI we can cite.

**Override semantics (verified empirically):** `phenotype_scale_overrides={"SLCO1B1": X}` replaces the *active* phenotype scale for the call, not the per-phenotype CPIC mapping. So passing override 0.30 to an EM individual (where CPIC default is 1.00×) would *reduce* their OATP1B1 abundance to 0.30× — the wrong behavior. The implementation in `genoadme.validate._real_pravastatin_simulator` therefore applies the override **only to PM individuals**, falling back to Sisyphus's `apply_phenotype_to_graph` defaults for EM (1.00×) and IM (0.50×). This is encoded in `genoadme.validate.PER_SUBSTRATE_PHENOTYPE_SCALES` keyed on `(gene, drug, phenotype_label)`.

**IM not separately calibrated:** under default 0.50× scaling, IM/EM AUC ratio is 1.313 (single-call) — already in line with Niemi 2006's reported "60–100% increase for heterozygous carriers" (target ratio 1.6–2.0). Adding an IM override would tighten further toward the upper of that range, but on single-study evidence this would be over-fitting. IM stays on CPIC default in v0.3.

**Audit-trail anchor:** the value 0.30 is committed alongside this section. Any future change requires a fresh `tier-change:` commit with new meta-analysis citation per [`docs/commit-discipline.md`](commit-discipline.md) §4.

> **Status update — superseded by §2.10 (PATH 3, 2026-05-09).** This calibration value (0.30) and the entire §2.8 framework was a P-ii path. Self-audit on 2026-05-09 identified that P-ii was solving the wrong problem; PATH 3 (P-i band re-spec) replaced it. §2.10 below records the reasoning. The override 0.30 has been REVERTED in code (`PER_SUBSTRATE_PHENOTYPE_SCALES = {}`); §2.8's content is preserved here as the audit trail of the rejected P-ii path.

### 2.9 Decision rule applied — original framing vs ultrathink-flagged concerns

The pre-spec decision rule (§1.5) was applied in §2.5 and produced verdict D-B + D-C: "the spec band [1.4, 2.5] is tighter than primary data with N≥4 PM directly supports". The v0.2 closing path P-ii (calibrate model to fit narrow band) and v0.3 P-ii closing (`a13d2d9`) both proceeded without revisiting this rule's implication. Self-audit (§2.10) revisited it.

### 2.10 PATH 3 — ultrathink-driven correction of the v0.3 closing

**Date:** 2026-05-09. **Trigger:** user request for ultra-deep self-audit of the v0.3 closing.

**Two findings from the audit (both verifiable, not subjective):**

1. **Citation error in the original v0.1.0 band rationale.** The pre-spec [`docs/validation-tiers.md`](validation-tiers.md) at v0.1.0 cited the band [1.4, 2.5] as derived from "Niemi 2006 and Pasanen 2007 clinical observations of 1.6–2.0×". Verification: (a) Pasanen 2007 is on atorvastatin / rosuvastatin, NOT pravastatin (verifiable by reading the Pasanen 2007 abstract or PMID 17473846); (b) Niemi 2006's accessible PM/EM AUC ratio data is the men-stratum 232% (CI 74–391%, ratio [1.74, 4.91]) — the "1.6–2.0×" range does not appear in primary CC-vs-TT N≥4 data and likely traces to review-aggregate summaries.

2. **The v0.3 P-ii calibration (override 0.30) was solving the wrong problem.** The post-PR-32 baseline model output (PM/EM AUC ratio 3.574, commit `8bc3517`) is INSIDE Niemi 2006 men-stratum 95% CI [1.74, 4.91]. The model is empirically consistent with primary data. The "over-shoot" framing assumed the band was correct; once the band is re-anchored to primary data (point 1 above), the model output is in-band without calibration.

**Implication:** the v0.3 closing path should have been P-i (band re-spec via citation correction), not P-ii (model calibration). The §2.6 recommendation of P-ii rested on a face-saving concern that turned out not to apply: P-i is correction of an identified citation error, not face-saving in the integrity-discipline sense.

**Action taken (PATH 3):**

- **`tier-change:` commit:** band [1.4, 2.5] → [1.4, 5.0] (encompasses Niemi 2006 men-stratum 95% CI upper, rounded). `PER_SUBSTRATE_PHENOTYPE_SCALES = {}` (override mechanism preserved in code as v0.4+ infrastructure but no current entries).
- **`metric-change:` commit:** validation re-run under new band, no override. PM/EM AUC ratio 3.574 PASSES the new band [1.4, 5.0]. AAFE PASSES. PM/EM Cmax PASSES. Verdict: PASS.
- **Prior v0.3 closing (commit `a13d2d9`, override 0.30, PM/EM AUC ratio 1.719) superseded** — preserved in git history; new dated report supersedes it explicitly.
- **v0.3 milestone description updated:** closing path documented as PATH 3 (P-i + tier-change band re-spec), not P-ii. Original closing condition ("without a tier-change band re-spec") was updated to reflect the corrected reasoning chain.

**Why this is correction, not face-saving:** the criteria for distinguishing correction from face-saving is whether the change is anchored on a *fact* about the original spec (correction) or on a *desire* to make results pass (face-saving). The two findings above are facts (Pasanen 2007's drug list; Niemi 2006's reported numbers; the model output's relationship to Niemi CI). Anchoring on facts → correction. The cost of *not* correcting is leaving the spec citing a paper that doesn't cover the drug in question, which is itself an integrity failure.

**Symmetry check:** is PATH 3 the LAST possible reframing? Honestly, no — further self-audit might find more nuance. But the current audit chain rests on object-level facts (Pasanen 2007 reading, Niemi 2006 reading), so any future reframing would need to challenge those facts. The current reasoning is robust against the kind of post-hoc rationalization that "from below" preference (§2.8) was.

**Sparse-evidence caveat preserved across PATH 3:** the new band's empirical anchor is the same single-study, sex-stratified, N(CC)=3 sub-cohort 95% CI used by the prior P-ii calibration target. The caveat doesn't disappear under PATH 3 — but its location is moved from *the model parameter* (override 0.30) to *the spec band* ([1.4, 5.0]). Any future tightening of the band requires fresh primary evidence with substantially larger N(CC).

**Lessons for future v0.X work:**

1. *Verify cited references before using them as a spec anchor.* A citation chain that traces back to a wrong-drug paper or a misread abstract is worse than a missing citation — it gives false confidence. The v0.3 meta-analysis caught this only because we re-derived the band's empirical anchor from primary data. A more disciplined pre-registration would have caught it earlier.
2. *When the model output is inside the empirical CI, don't calibrate the model.* The instinct to "fix the model" because criteria don't pass should be tempered by checking whether the criteria themselves match the empirical CI. If they don't, fix the criteria.
3. *"Conservative" is direction-dependent.* The §2.8 "from below" preference was framed as conservative but actually under-predicted the SLCO1B1 effect. For a PGx tool whose role is to *inform about* genotype effects, under-prediction is anti-conservative — it makes the genotype effect look smaller than it is. Future calibration choices should explicitly reason about which direction "conservative" cuts.

**Audit-trail anchor:** the band [1.4, 5.0] and the empty `PER_SUBSTRATE_PHENOTYPE_SCALES` are committed alongside this section under the 2026-05-09 PATH 3 `tier-change:` commit. Any future change requires fresh `tier-change:` discipline per [`docs/commit-discipline.md`](commit-discipline.md) §4.

-----

## 3. Search execution log

### 3.1 Sources fetched successfully

- `https://pubmed.ncbi.nlm.nih.gov/17015053/` — Niemi 2006 (PRIMARY).
- `https://pmc.ncbi.nlm.nih.gov/articles/PMC4063287/` — Ho 2007 full-text via PMC; Table 3 extracted.
- `https://pubmed.ncbi.nlm.nih.gov/17177112/` — Kivistö & Niemi 2007 (review, no primary data).
- `https://pubmed.ncbi.nlm.nih.gov/19776292/` — Ide 2009 (population-PK; excluded).
- `https://pubmed.ncbi.nlm.nih.gov/26744986/` — Lu 2016 (population-PK; excluded).
- `https://pubmed.ncbi.nlm.nih.gov/15116054/` — Mwinyi 2004; Table extracted.
- `https://pubmed.ncbi.nlm.nih.gov/15226675/` — Niemi 2004 (insufficient detail).
- `https://pubmed.ncbi.nlm.nih.gov/12811365/` — Nishizato 2003 (CL/F, N(PM)=1).
- `https://pubmed.ncbi.nlm.nih.gov/16678544/` — Igel 2006 (multiple-dose; excluded).
- `https://pubmed.ncbi.nlm.nih.gov/16678545/` — Maeda 2006 (no CC group).
- `https://pubmed.ncbi.nlm.nih.gov/16722833/` — Hedman 2006 (paediatric; excluded).
- `https://pmc.ncbi.nlm.nih.gov/articles/PMC8159730/` — meta-analysis of myopathy outcomes only (no PK pooling).
- `https://pmc.ncbi.nlm.nih.gov/articles/PMC10900141/` — PharmVar GeneFocus (narrative review, no PK table).

### 3.2 Sources attempted but inaccessible

- `https://files.cpicpgx.org/data/guideline/publication/statins/2022/supplement.pdf` — PDF binary, content not extractable through WebFetch's text pipeline. Disclosed as an extraction limitation; CPIC's curated summary tables would likely have been the most efficient corroborating evidence source.
- `https://www.researchgate.net/publication/7101648_…` (Maeda 2006) — 403 (paywall / blocked).
- `https://cpicpgx.org/guidelines/cpic-guideline-for-statins-and-slco1b1-abcg2-and-cyp2c9/` — 404. The CPIC guidelines page URL has changed; not pursued further.
- `https://www.sciencedirect.com/topics/pharmacology-toxicology-and-pharmaceutical-science/slco1b1` — 403.

### 3.3 Search-protocol limitations summary

- Full-text access was best-effort. Where only abstracts were accessible (Niemi 2006, Niemi 2004, Maeda 2006), extraction is `medium` confidence per the §1.4 schema.
- The CPIC supplement PDF being unparseable removed the most likely curated-data source.
- bioRxiv MCP not queried. Disclosed as a completeness limitation.
- Single-rater extraction (no second-rater check) — not an honest "meta-analysis" by formal Cochrane standards. Disclosed in §1.6.

-----

## 3. Search execution log

*Captured during execution. One subsection per query attempted.*
